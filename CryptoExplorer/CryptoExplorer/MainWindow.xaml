<Window x:Class="CryptoExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:constants="clr-namespace:CryptoExplorer"
        mc:Ignorable="d"
        Title="CryptoExplorer" MinHeight="450" MinWidth="800">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding OnLoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <!--    <Frame prism:RegionManager.RegionName="ContentRegionMainWindow" NavigationUIVisibility="Hidden">


    </Frame> -->

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.25*"/>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical"
                    Grid.Column="0">
            <Button Width="100" Height="30" Margin="20">
                <TextBlock Text="MainPage" Foreground="Red" FontSize="18"/>
            </Button>
            <Button Width="100" Height="30" Margin="20">
                <TextBlock Text="Graphics" Foreground="Red" FontSize="18"/>
            </Button>
            <Button Width="100" Height="30" Margin="20">
                <TextBlock Text="Settings" Foreground="Red" FontSize="18"/>
            </Button>

            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="Settings"
                           Background="Blue"
                           Foreground="Black"
                           FontSize="18"
                           VerticalAlignment="Center">
                    
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <prism:InvokeCommandAction Command="{Binding NavigateCurrencyDetailsPageCommand}"
                                                   CommandParameter="{x:Static constants:Constants.CURRENCY_DETAILS_PAGE}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                </TextBlock>
            </StackPanel>


        </StackPanel>

        <ContentControl Grid.Column="1"
                        prism:RegionManager.RegionName="ContentRegion"
                        Margin="5" />

    </Grid>

    <!--<ContentControl prism:RegionManager.RegionName="ContentRegion" 
    
            xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"/>-->

</Window>
