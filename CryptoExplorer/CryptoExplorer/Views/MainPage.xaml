<UserControl x:Class="CryptoExplorer.Views.MainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:CryptoExplorer.Converters" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:NumberConverter x:Key="numberConverter"/>
    </UserControl.Resources>
    <Grid>
        
        <ListView Name="currenciesList"
                  ItemsSource="{Binding СurrencyList}"
                  SelectedItem="{Binding Currency}">
            
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Name}">Currency</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Symbol}">Symbol</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=PriceUsd, Converter={StaticResource numberConverter}}">Price</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=VolumeUsd24Hr}">VolumeUsd24Hr</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Vwap24Hr}">Vwap24Hr</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=MaxSupply}" Width="100">MaxSupply</GridViewColumn>
                </GridView>
            </ListView.View>
            
        </ListView>
        <!--<TextBlock Text="View A" FontSize="38"/> 
        <Button Command="{Binding GetGetCurrenciesCommand}" Width="100" Height="50" Background="Blue"/>-->

    </Grid>
</UserControl>
