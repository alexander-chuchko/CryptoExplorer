<UserControl x:Class="CryptoExplorer.Controls.CustomConverter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CryptoExplorer.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Border BorderBrush="Gray" 
                BorderThickness="1"
                CornerRadius="1">

            <Grid Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0"
                                   VerticalAlignment="Stretch"
                                   HorizontalAlignment="Stretch" 
                                   Margin="0"
                                   Source="/images/dollar_light.png">
                </Image>

                <TextBlock Grid.Column="1"
                                           Grid.Row="0"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="22"
                                           FontWeight="DemiBold"
                                           Foreground="#596EFB"
                                           Text="USD"/>


                <Image Grid.Column="2" Grid.Row="0" Margin="5">
                    <Image.Source>
                        <BitmapImage UriSource="/images/up_arrow_light.png" Rotation="Rotate0"/>
                    </Image.Source>
                </Image>

                <TextBlock Text="|" 
                           HorizontalAlignment="Center"
                           Grid.Column="3"
                           Margin="0,0,0,7"
                           Foreground="Gray"
                           FontSize="30"
                           VerticalAlignment="Center"/>

                <TextBox Background="#C0FFFFFF"
                         Grid.Column="4"
                         FontSize="22" 
                         MaxLength="30"
                         Foreground="#596EFB"
                         FontWeight="DemiBold"
                         BorderThickness="0"
                         VerticalAlignment="Center"
                         VerticalContentAlignment="Center">
                </TextBox>

                <Image Grid.Column="5"
                       VerticalAlignment="Stretch"
                       HorizontalAlignment="Stretch" 
                       Margin="0"
                       Source="/images/ic_clear.png">
                </Image>
                
            </Grid>
        </Border>
    </Grid>
</UserControl>
